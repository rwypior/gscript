TODOs
=====
- Make VariableAccessors find addresses during compilation
- Add test for nested scopes
- Remove setScope() from variable/function accessors and everything that's related to it

Fixes
=====

- add support for returning references from functions:
	- add syntax for '&' character after type in function parsing
	- set the appropriate parameter for compiler when parsing detected that character
	- add tests for it

Framework stuff
===============

- Improve error reporting
- Add tests for reference values
- See if ScriptValue's getType() can return unique_ptr, or something else than shared_ptr
- VariableAccessor and FunctionAccessor should be treated as prototypes and return real addresses after built
- Add serialization/deserialization classes which will enable quick loading pre-parsed scripts
	- Create unit tests for that
- Find and eliminate raw pointer usage
	- ScriptCallable is returning raw pointer - it should return std::shared_ptr instead
	- ScriptVariable is taking raw pointer as default value - it should take std::unique_ptr (?) instead
- Remove comment parsing from reduntant places
	- There may be some comment parsing where it's no longer required - it should be enough to parse
		for comments ONLY BEFORE AND AFTER any parser entity, and the rest (inbetween stuff) should work itself out

Functionality
=============

for ... else
Syntax would be:
```
	for (int i = 0; i < 5; i++)
	{
	}
	else
	{
		// Something that runs if the loop doesn't run at least once
	}
```

----------------------

while ... else
do ... while ... else
Samething as for for ... else, but for while loops, same syntax

----------------------

events
Make an universal event object, usage would be like in cppevents lib

----------------------

attributes
Add an attribute syntax - something like [[nodiscard]] in C++