- Move ParserEntity::StringIteratorRange to separate file
- Fix includes after refactoring
- Add factory classes which will create runtime statements from parser statements
- Create unit tests
- Add serialization/deserialization classes which will enable quick loading pre-parsed scripts
- Create unit tests for that
- Remove that weird access control class from runtime and add it to parser

======

- RETURN statement
- class member syntax:
  - class.function()
  - class.variable
- operators
- execution control
  - if
  - for
  - while
  - do ... while
  - foreach
  - break
  - continue
- arrays
- creating class objects
  - assign class object with CLASS type and store as variable
  - call constructor upon object creation
- access control
  - private
  - public
  - protected
- refactor
- change returning raw pointers to shared_ptr